import data from './data.js'

const pages = {
	education: edu,
	experience: exp,
	manual: man,
	projects: pro,
	sources: src
}

const frag = document.createFragment()

document.querySelector("aside").append()
